---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: {{ fluxcd_traefik_release_name }}
  namespace: {{ fluxcd_namespace }}
spec:
  interval: 5m
  path: {{ fluxcd_git_path_traefik }}
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: {{ fluxcd_release_name }}
