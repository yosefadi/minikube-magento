---
# Testing inventory — diverged from demo because testing uses --driver=none
# while demo uses the default (docker).  The resource values below should still
# match demo's as closely as possible so this environment can rehearse what the
# demo host will get (driver choice being the deliberate exception).
#
# Sized for a host with ~3.9-4.0 GiB RAM / 2 vCPUs / enough disk for 50g (both
# the testing Hyper-V VM and the demo Linode box were confirmed to fit this).
minikube_driver: none
minikube_cpus: 2
minikube_memory: 2800
minikube_disk_size: 50g
minikube_extra_args:
  - --force
  - --extra-config=apiserver.feature-gates=ExtendWebSocketsToKubelet=false

# Leave swap on and have Kubernetes actually manage it (KEP-2400) instead of
# either the unmanaged fallback minikube uses by default, or disabling it.
common_disable_swap: false
minikube_enable_limited_swap: true

minikube_addons:
  - default-storageclass
  - storage-provisioner
  - metrics-server

# Enabled deliberately on both — for demo, this is config-only until a
# Cloud Firewall / security group check happens; do not run this playbook
# against the demo Linode host while this is true without doing that check.
minikube_expose_apiserver: true

# GitOps overlay path for this environment
fluxcd_git_path: ./kubernetes/gitops/overlays/testing
