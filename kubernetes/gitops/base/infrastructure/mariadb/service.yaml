---
apiVersion: v1
kind: Service
metadata:
  name: mariadb
  namespace: infrastructure
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mariadb
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
