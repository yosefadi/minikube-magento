---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: magento
  namespace: apps
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: magento
  template:
    metadata:
      labels:
        app.kubernetes.io/name: magento
    spec:
      initContainers:
        - name: init-app-etc
          image: ghcr.io/yosefadi/minikube-magento/magento:2.4.9-88957dd
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              if [ ! -f /mnt/app-etc/di.xml ]; then
                echo "app/etc PVC is empty — seeding from image..."
                cp -a /var/www/html/app/etc/. /mnt/app-etc/
              fi
          volumeMounts:
            - name: app-etc
              mountPath: /mnt/app-etc
      containers:
        - name: magento
          image: ghcr.io/yosefadi/minikube-magento/magento:2.4.9-88957dd
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: magento-config
          env:
            - name: MAGENTO_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: magento-credentials
                  key: db-password
            - name: MAGENTO_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: magento-credentials
                  key: admin-password
            - name: MAGENTO_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: magento-credentials
                  key: admin-user
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              memory: 512Mi
          volumeMounts:
            - name: app-etc
              mountPath: /var/www/html/app/etc
            - name: pub-static
              mountPath: /var/www/html/pub/static
            - name: pub-media
              mountPath: /var/www/html/pub/media
      volumes:
        - name: app-etc
          persistentVolumeClaim:
            claimName: magento-etc
        - name: pub-static
          persistentVolumeClaim:
            claimName: magento-static
        - name: pub-media
          persistentVolumeClaim:
            claimName: magento-media
